---
phase: 01-core-commands
plan: 06
type: execute
wave: 3
depends_on: [01-03, 01-04]
files_modified:
  - src/commands/revert.test.js
  - src/commands/redo.test.js
  - src/commands/gc.test.js
autonomous: true
requirements: [D-01, D-02, D-03, D-08, D-09, D-10, D-12]

must_haves:
  truths:
    - "node --test src/commands/revert.test.js passes all tests"
    - "node --test src/commands/redo.test.js passes all tests"
    - "node --test src/commands/gc.test.js passes all tests"
    - "revert test: after revert N, a tracked file matches the snapshot from turn N"
    - "revert test: HEAD sha is unchanged after revert (per D-01)"
    - "revert test: revert fails with branch mismatch message when on wrong branch (per D-03)"
    - "revert test: a pre-revert snapshot SHA is written to the op log before restoring"
    - "redo test: after revert then redo, the tracked file content is back to the pre-revert state"
    - "redo test: calling redo with no prior revert exits 1 with 'nothing to redo'"
    - "redo test: calling redo twice after one revert fails on second call (op consumed)"
    - "gc test: refs for sessions beyond keepN are deleted"
    - "gc test: current session is never pruned"
    - "gc test: gc hint string is printed when pruning occurs; not printed when nothing to prune"
  artifacts:
    - path: "src/commands/revert.test.js"
      provides: "tests for revert command (D-01, D-03)"
    - path: "src/commands/redo.test.js"
      provides: "tests for redo command (D-02)"
    - path: "src/commands/gc.test.js"
      provides: "tests for gc command (D-08, D-09, D-10)"
  key_links:
    - from: "src/commands/revert.test.js"
      to: "src/commands/revert.js"
      via: "require('./revert').run(args)"
      pattern: "run\\(\\['"
    - from: "src/commands/redo.test.js"
      to: "src/commands/redo.js + revert.js"
      via: "revert then redo call sequence"
      pattern: "revert.*run.*redo.*run"
    - from: "src/commands/gc.test.js"
      to: "src/git.js listRefs()"
      via: "require('../git').listRefs to verify refs deleted"
      pattern: "listRefs\\("
---

<objective>
Write tests for the stateful revert, redo, and gc commands using real temp git repos.

Purpose: These commands modify worktree state and refs — they require integration tests against a real git repo. Wave 3 runs after revert.js (01-03) and gc.js (01-04) exist. Together with 01-05, these test files complete the D-12 test suite requirement.

Output: src/commands/revert.test.js, src/commands/redo.test.js, src/commands/gc.test.js.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-core-commands/01-CONTEXT.md
@.planning/phases/01-core-commands/01-RESEARCH.md
@.planning/phases/01-core-commands/01-PATTERNS.md
@.planning/phases/01-core-commands/01-03-SUMMARY.md
@.planning/phases/01-core-commands/01-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write revert and redo tests (D-01, D-02, D-03)</name>
  <files>src/commands/revert.test.js, src/commands/redo.test.js</files>
  <read_first>
    - src/commands/revert.js — the exact flow: snapshotWorktree → writeOp('revert', ...) → restoreSnapshot(); field names pre_revert_snapshot_sha and branch in op payload
    - src/commands/redo.js — the LIFO search pattern, redeemedKeys matching on session_id + turn_n, writeOp('redo', ...) after restore
    - src/session.js — readOps(), writeOp(), readSession(), newSession(), incrementTurn(), turnRef() — all needed to set up multi-turn test state
    - src/git.js — snapshotWorktree(), updateRef(), restoreSnapshot() — needed for manual turn creation in tests
    - src/helpers/test-repo.js — makeTestRepo() interface
  </read_first>
  <action>
Create src/commands/revert.test.js.

Imports: 'use strict'. Import describe, test, beforeEach, afterEach from 'node:test'. Import assert from 'node:assert/strict'. Import makeTestRepo from '../helpers/test-repo'. Import rmSync, writeFileSync, readFileSync, mkdirSync, existsSync from 'fs'. Import { join } from 'path'.

Helper function makeTurn(session, content) defined inside the test file (not exported): writes content to 'work.txt' in process.cwd(), calls git(['add', '.']), calls git(['commit', '-q', '-m', 'turn ' + (session.turn_n + 1)]) using the test repo's git helper passed in. Then calls snapshotWorktree({ message: 'turn ' + (session.turn_n + 1) }). Then updateRef(turnRef(session.session_id, session.turn_n + 1), commitSha). Then incrementTurn(session) and returns commitSha. Note: git helper from makeTestRepo is needed for commit calls; snapshotWorktree/updateRef/turnRef/incrementTurn are from src modules (process.cwd() is already the temp repo).

beforeEach pattern: origCwd = process.cwd(); { dir, git } = makeTestRepo(); process.chdir(dir); then ensureDirs(); session = newSession() — store git and session in test-scope variables.

afterEach: process.chdir(origCwd); rmSync(dir, { recursive: true, force: true }).

Tests for revert.test.js (describe 'revert command'):
1. 'restores worktree to turn N content without changing HEAD (D-01)': make 2 turns (content 'v1' and 'v2'). Record headBefore = git(['rev-parse', 'HEAD']). run(['1']). Assert readFileSync(join(process.cwd(), 'work.txt'), 'utf8').trim() === 'v1'. Assert git(['rev-parse', 'HEAD']) === headBefore (HEAD unchanged per D-01).
2. 'writes pre_revert_snapshot_sha to op log before restoring': make 1 turn. run(['1']). ops = readOps(). revertOp = ops.find(op => op.type === 'revert'). assert.ok(revertOp). assert.ok(revertOp.pre_revert_snapshot_sha, 'pre-revert snapshot SHA must exist in op log').
3. 'fails with branch mismatch when on different branch (D-03)': make 1 turn. Create a new branch: git(['checkout', '-b', 'other']). Run revert for a session that has branch = original branch. Assert run(['1']) throws (or call via try/catch) with a message containing 'Branch mismatch' or 'branch'.

Tests for redo.test.js (describe 'redo command'):
1. 'restores pre-revert state after revert (D-02)': make 2 turns ('v1', 'v2'). revert to turn 1 (content becomes 'v1'). Run redo. Assert readFileSync('work.txt', 'utf8').trim() includes 'v2' (restored to pre-revert state which was 'v2').
2. 'exits 1 with nothing to redo when no revert in op log': attempt run redo with no prior revert. Capture exit code (mock process.exit or test via try/catch). Assert 'nothing to redo' is in error output or process.exit called with 1.
3. 'second redo fails after first redo succeeds (op consumed)': make 2 turns. Revert. Redo (success). Run redo again. Assert it exits 1 with 'nothing to redo'.

All process.chdir calls must be inside beforeEach/afterEach — never at module level.
  </action>
  <verify>
    <automated>node --test src/commands/revert.test.js && node --test src/commands/redo.test.js</automated>
  </verify>
  <acceptance_criteria>
    - node --test src/commands/revert.test.js exits 0
    - node --test src/commands/redo.test.js exits 0
    - revert test checks readFileSync to assert file content matches turn N state
    - revert test checks HEAD is unchanged after revert (git rev-parse HEAD same before and after)
    - revert test checks pre_revert_snapshot_sha field exists in the revert op
    - redo test performs revert-then-redo cycle and asserts file content is restored
    - redo test verifies second redo fails (op consumed)
    - grep -c 'beforeEach' src/commands/revert.test.js returns at least 1
    - grep -c 'afterEach' src/commands/revert.test.js returns at least 1
  </acceptance_criteria>
  <done>revert.test.js tests D-01 (HEAD unchanged), D-03 (branch mismatch fails), and pre-revert snapshot op write. redo.test.js tests D-02 LIFO redo, nothing-to-redo, and double-redo fails.</done>
</task>

<task type="auto">
  <name>Task 2: Write gc tests (D-08, D-09, D-10)</name>
  <files>src/commands/gc.test.js</files>
  <read_first>
    - src/commands/gc.js — just created; understand how it enumerates sessions via for-each-ref --sort=creatordate; how it calls deleteRef per turn ref
    - src/git.js — listRefs(prefix): used in tests to verify refs were deleted; updateRef(ref, sha): used to manually create session refs in test setup
    - src/session.js — turnRef(sessionId, turnN): ref name pattern 'refs/git-turn/sessions/{id}/turn-{N}'
  </read_first>
  <action>
Create src/commands/gc.test.js.

Imports: 'use strict'. Import describe, test, beforeEach, afterEach from 'node:test'. Import assert from 'node:assert/strict'. Import makeTestRepo from '../helpers/test-repo'. Import rmSync from 'fs'. Import { join } from 'path'.

Helper makeSessionRefs(sessionId, turnCount, git, dir) inside test file: creates N turn refs for a given sessionId by writing dummy commits and calling updateRef(turnRef(sessionId, n), headSha) for n=1..N. Use headSha() from '../git' for the SHA. Add a 10ms delay or multiple commits to ensure different creatordate values — or make actual commits so creatordate increments: git(['commit', '--allow-empty', '-m', 'turn ' + n]).

Test setup strategy for gc: create 3 sessions manually by calling newSession() → making commits → then calling writeSession() to update session_id to a new UUID for the next session. Simplest: write session refs directly via updateRef without needing a full session workflow. Use git(['commit', '--allow-empty', '-m', 'seed']) to get a valid SHA, then updateRef(turnRef(uuid, 1), sha).

beforeEach: origCwd; { dir, git } = makeTestRepo(); process.chdir(dir); session = newSession().
afterEach: process.chdir(origCwd); rmSync(dir, ...).

Tests for gc.test.js (describe 'gc command'):

1. 'does nothing when session count is at or below keepN': create 2 sessions with 1 turn each. run(['--keep-sessions', '3']). Assert listRefs('refs/git-turn/sessions/') still has 2 entries.

2. 'prunes oldest sessions when count exceeds keepN (D-08, D-09)': create 3 fake sessions (session IDs A, B, C) with turn refs. run(['--keep-sessions', '2']). Assert refs for session A (oldest) are gone: listRefs('refs/git-turn/sessions/' + idA + '/') returns []. Assert refs for sessions B and C still exist.

3. 'never prunes current active session even if it is the oldest': write session.json pointing at session A (oldest). Create 11 sessions. run([]) (default keepN=10). listRefs for session A must NOT be empty — current session is protected.

4. 'prints gc hint after pruning (D-10)': capture console.log output during run(). After pruning occurs, assert output includes "Snapshot objects will be reclaimed on next 'git gc'".

5. 'does not print gc hint when nothing pruned': only 1 session, run(['--keep-sessions', '10']). Assert output does NOT include 'Snapshot objects will be reclaimed'.

For session age ordering (Pitfall 6): the test creates sessions with actual git commits so creatordate is real. If using updateRef with a HEAD sha, creatordate reflects the actual commit time. Create sessions sequentially with brief pauses or use --allow-empty commits to ensure creatordate differs. A simple approach: loop to create N sessions, each with a new UUID and one commit.

All process.chdir inside beforeEach/afterEach only.
  </action>
  <verify>
    <automated>node --test src/commands/gc.test.js</automated>
  </verify>
  <acceptance_criteria>
    - node --test src/commands/gc.test.js exits 0
    - Tests verify refs for pruned sessions are absent via listRefs returning []
    - Test verifies current session is never pruned (test 3)
    - Test captures console.log to assert D-10 hint string appears when pruning and does not appear when nothing pruned
    - grep -c 'beforeEach' src/commands/gc.test.js returns at least 1
    - grep -c 'afterEach' src/commands/gc.test.js returns at least 1
    - grep -c "Snapshot objects will be reclaimed" src/commands/gc.test.js returns at least 1 (D-10 hint verified in test)
  </acceptance_criteria>
  <done>gc.test.js passes with node --test; covers default keepN=10 (D-08), ref-only deletion with listRefs verification (D-09), current-session protection, and D-10 hint output both when pruned and when not pruned.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test process → temp git repo | Tests run git commands against isolated temp repos; HOME override prevents config leakage |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Tampering | test isolation | mitigate | HOME: dir in makeTestRepo; chdir in beforeEach/afterEach; rmSync in afterEach |
| T-06-02 | Denial of Service | gc test current-session protection | mitigate | Test 3 explicitly verifies current session is never pruned, even when it is the oldest |
| T-06-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed in this plan |
</threat_model>

<verification>
After both tasks complete:
- node --test src/commands/revert.test.js exits 0
- node --test src/commands/redo.test.js exits 0
- node --test src/commands/gc.test.js exits 0
- node --test (full suite from repo root) exits 0 — all 6 test files discovered and passing
</verification>

<success_criteria>
- revert.test.js covers: worktree restore, HEAD unchanged (D-01), branch mismatch fail (D-03), pre-revert snapshot in op log
- redo.test.js covers: LIFO redo restores pre-revert state (D-02), nothing-to-redo, double-redo fails (op consumed)
- gc.test.js covers: session pruning (D-08, D-09), current-session protection, D-10 hint string
- All test files: process.chdir inside beforeEach/afterEach only (never at module level per Pitfall 7)
- node --test (full suite) passes: 0 failures
</success_criteria>

<output>
Create .planning/phases/01-core-commands/01-06-SUMMARY.md when done
</output>
