---
phase: 01-core-commands
plan: 05
type: execute
wave: 3
depends_on: [01-01, 01-02]
files_modified:
  - src/commands/diff.test.js
  - src/commands/status.test.js
  - src/commands/init.test.js
autonomous: true
requirements: [D-04, D-05, D-06, D-07, D-11, D-12]

must_haves:
  truths:
    - "node --test src/commands/diff.test.js passes all tests"
    - "node --test src/commands/status.test.js passes all tests"
    - "node --test src/commands/init.test.js passes all tests"
    - "diff test: diff N M outputs patch between snapshots of turn N and turn M"
    - "diff test: diff N with N=1 diffs against the empty tree"
    - "diff test: diff N with N>1 diffs against turn N-1 snapshot"
    - "status test: output includes session ID, branch, turn count, started timestamp, and hook status on separate lines"
    - "status test: hook status is 'installed' when hook file contains 'git-turn'"
    - "status test: hook status is 'missing' when hook file does not exist"
    - "init test: refs/git-turn/format resolves after git turn init; git cat-file blob returns JSON with version='1' and tool='git-turn'"
  artifacts:
    - path: "src/commands/diff.test.js"
      provides: "tests for diff command (D-04, D-05)"
    - path: "src/commands/status.test.js"
      provides: "tests for status command (D-06, D-07)"
    - path: "src/commands/init.test.js"
      provides: "tests for init format blob (D-11)"
  key_links:
    - from: "src/commands/diff.test.js"
      to: "src/helpers/test-repo.js"
      via: "require('../helpers/test-repo')"
      pattern: "makeTestRepo\\("
    - from: "src/commands/init.test.js"
      to: "src/commands/init.js"
      via: "require('./init').run([])"
      pattern: "run\\(\\[\\]\\)"
---

<objective>
Write tests for diff, status, and init using the shared test-repo helper and real temp git repos.

Purpose: These three commands are read-only or init-only — tests can run without needing revert/redo to exist. Wave 3 runs after all command files are created (waves 1 and 2). D-12 mandates node:test runner with src/**/*.test.js auto-discovery.

Output: src/commands/diff.test.js, src/commands/status.test.js, src/commands/init.test.js. All use makeTestRepo() from src/helpers/test-repo.js.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-core-commands/01-CONTEXT.md
@.planning/phases/01-core-commands/01-RESEARCH.md
@.planning/phases/01-core-commands/01-PATTERNS.md
@.planning/phases/01-core-commands/01-01-SUMMARY.md
@.planning/phases/01-core-commands/01-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write diff and status tests (D-04, D-05, D-06, D-07, D-12)</name>
  <files>src/commands/diff.test.js, src/commands/status.test.js</files>
  <read_first>
    - src/helpers/test-repo.js — makeTestRepo() interface: returns { dir, git }; git(args) runs in dir with HOME=dir
    - src/commands/diff.js — run(args) interface; args[0] = N string, args[1] = M string (both as strings, not ints)
    - src/commands/status.js — run([]) interface; no args; reads process.cwd() for session path via session.js
    - src/session.js — ensureDirs(), newSession(), writeSession() — needed to set up a session in the test repo before calling commands
    - src/hooks/post-commit.js — post-commit creates snapshot refs; can be invoked directly or simulated by running git commit in test repo with hook installed
    - RESEARCH.md code example (lines 443-490) — beforeEach/afterEach chdir pattern, process.env.HOME override rationale
  </read_first>
  <action>
Create src/commands/diff.test.js.

Imports at top of file: 'use strict'. Import describe, test, beforeEach, afterEach from 'node:test'. Import assert from 'node:assert/strict'. Import makeTestRepo from '../helpers/test-repo'. Import rmSync from 'fs'. Import { join } from 'path'. Import { run } from './diff'.

Key test setup pattern: beforeEach captures origCwd = process.cwd(). Creates a test repo with makeTestRepo(). Calls process.chdir(dir) INSIDE beforeEach (never at module level). Sets up git turn session by: require('../session').ensureDirs(); require('../session').newSession() — this creates session.json in the temp repo's .git/git-turn/. afterEach: process.chdir(origCwd); rmSync(dir, { recursive: true, force: true }).

To create turns for testing: write a file, git add, git commit, then call require('../hooks/post-commit').run() and wait for the pending promise to settle. Simpler alternative: directly call snapshotWorktree from src/git.js, then updateRef to set the turn ref manually using turnRef() from session.js + incrementTurn(). Use the manual approach to avoid async hook complexity.

Helper inside test file: function makeTurn(session, git, dir, content) — writes a file, stages, commits, then calls snapshotWorktree({ message: 'turn N' }) and updateRef(turnRef(session.session_id, session.turn_n + 1), commitSha), then incrementTurn(session) and returns the commitSha.

Tests for diff.test.js (all inside a describe('diff command') block):
1. 'exits 1 when not initialized': chdir to a plain temp dir with no session; assert.throws or capture process.exit call. Simpler: in a separate tmp dir (no session.json), process.chdir, then check that running run(['1']) causes process.exit(1) — use try/catch on process.exit mock or check stderr. Practical approach: directly check that readSession() returns null in that context and the run function calls process.exit(1) — use a mock of process.exit. OR skip process.exit mocking and document that this test verifies behavior by inspecting the command source. Recommended: spy on process.exit and assert it was called with 1.
2. 'diff 1 uses empty tree when only one turn': make 1 turn; run(['1']); capture console.log output; assert output includes a diff hunk or '(no changes)' (both are valid — key is no error thrown).
3. 'diff N M diffs turn N snapshot against turn M snapshot' (D-04): make 2 turns with different file contents; run(['1', '2']); capture stdout; assert output is a string (truthy or '(no changes)').

For capturing stdout in tests: replace console.log temporarily with a collector before calling run(), restore after.

Tests for status.test.js (all inside describe('status command')):
1. 'prints session ID, branch, turns, started, hook status': make a test repo, set up session, call run([]); capture output; assert output includes session.session_id substring, 'Branch:', 'Turns:', 'Started:', 'Hook:'.
2. 'hook shows installed when hook file contains git-turn': write .git/hooks/post-commit with content '#!/bin/sh\n# git-turn\n'; run([]); assert output includes 'installed'.
3. 'hook shows missing when hook file absent': ensure no post-commit file; run([]); assert output includes 'missing'.
4. 'hook shows not from git-turn when hook exists but lacks git-turn': write .git/hooks/post-commit with '#!/bin/sh\necho hello'; run([]); assert output includes 'not from git-turn'.

All process.chdir calls MUST be inside beforeEach/afterEach — never at module level.
  </action>
  <verify>
    <automated>node --test src/commands/diff.test.js && node --test src/commands/status.test.js</automated>
  </verify>
  <acceptance_criteria>
    - node --test src/commands/diff.test.js exits 0 (all tests pass)
    - node --test src/commands/status.test.js exits 0 (all tests pass)
    - grep -c 'beforeEach' src/commands/diff.test.js returns at least 1
    - grep -c 'afterEach' src/commands/diff.test.js returns at least 1
    - grep -v 'beforeEach\|afterEach' src/commands/diff.test.js | grep -c 'process.chdir' returns 0 (chdir only in beforeEach/afterEach)
    - grep -c 'makeTestRepo' src/commands/diff.test.js returns at least 1
    - grep -c 'makeTestRepo' src/commands/status.test.js returns at least 1
    - status test file contains tests for 'installed', 'missing', and 'not from git-turn' hook states
  </acceptance_criteria>
  <done>diff.test.js and status.test.js pass with node --test; chdir is per-test inside beforeEach/afterEach; all three hook states are covered in status tests.</done>
</task>

<task type="auto">
  <name>Task 2: Write init format blob test (D-11, D-12)</name>
  <files>src/commands/init.test.js</files>
  <read_first>
    - src/commands/init.js — just modified to write format blob; understand what git turn init does: installs hook, sets notes.expiry, creates session, writes format blob; run([]) is synchronous
    - src/git.js — gitSafe() to read blob content in test: gitSafe(['cat-file', 'blob', 'refs/git-turn/format']) returns the JSON string
    - src/helpers/test-repo.js — makeTestRepo() interface
  </read_first>
  <action>
Create src/commands/init.test.js.

Imports: 'use strict'. Import describe, test, beforeEach, afterEach from 'node:test'. Import assert from 'node:assert/strict'. Import makeTestRepo from '../helpers/test-repo'. Import rmSync, writeFileSync, mkdirSync, existsSync from 'fs'. Import { join } from 'path'.

Setup: beforeEach captures origCwd. Creates test repo with makeTestRepo(). process.chdir(dir) inside beforeEach. afterEach: process.chdir(origCwd); rmSync(dir, ...).

Test 1 — 'init writes format blob to refs/git-turn/format': call require('./init').run([]) inside the chdir'd test repo. Then call require('../git').gitSafe(['cat-file', 'blob', 'refs/git-turn/format']) to read the blob content. Assert the result is not null. JSON.parse the result. Assert parsed.version === '1'. Assert parsed.tool === 'git-turn'.

Test 2 — 'init does not write refs/turns/.format': call run([]). Then call require('../git').gitSafe(['rev-parse', 'refs/turns/.format']). Assert the result is null (gitSafe returns null on error; this ref must not exist).

Test 3 — 'init creates session.json': call run([]). Assert existsSync(join('.git', 'git-turn', 'session.json')).

Test 4 — 'init is idempotent when hook already installed': call run([]) twice. Assert second call does not throw (already-installed message is printed, not an error exit).

Notes: require('./init') must be called fresh for each test — use delete require.cache[require.resolve('./init')] before each require call to ensure module state is reset, OR require once at file top (module is stateless). The init module is stateless so a single require at top is fine.
  </action>
  <verify>
    <automated>node --test src/commands/init.test.js</automated>
  </verify>
  <acceptance_criteria>
    - node --test src/commands/init.test.js exits 0
    - Test file includes assertion that parsed JSON has version === '1' (grep -c "version.*'1'" or "=== '1'" src/commands/init.test.js returns at least 1)
    - Test file includes assertion that parsed JSON has tool === 'git-turn'
    - Test file includes assertion that refs/turns/.format does NOT exist (null check)
    - grep -c 'beforeEach' src/commands/init.test.js returns at least 1
    - grep -c 'process.chdir' src/commands/init.test.js — all occurrences are inside beforeEach or afterEach callbacks (module-level chdir forbidden per Pitfall 7)
  </acceptance_criteria>
  <done>src/commands/init.test.js passes with node --test; verifies format blob written to refs/git-turn/format (not refs/turns/.format) with correct JSON fields per D-11.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test process → temp git repo | Each test creates an isolated temp dir; HOME is overridden to dir to prevent global config bleed |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Tampering | test HOME isolation | mitigate | makeTestRepo sets HOME: dir in git env; prevents signing keys or credential helpers from interfering with test commits |
| T-05-02 | Information Disclosure | test temp dir cleanup | mitigate | afterEach calls rmSync(dir, { recursive: true }); temp repos with any sensitive config are deleted |
| T-05-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed in this plan |
</threat_model>

<verification>
After both tasks complete:
- node --test src/commands/diff.test.js exits 0
- node --test src/commands/status.test.js exits 0
- node --test src/commands/init.test.js exits 0
- node --test (full suite) exits 0 with no "no matches found" error
</verification>

<success_criteria>
- All three test files pass with node --test
- chdir is per-test in beforeEach/afterEach in all files (never at module level)
- init test verifies refs/git-turn/format exists with correct JSON and refs/turns/.format does not exist
- status test covers all three hook states: installed, missing, not from git-turn
</success_criteria>

<output>
Create .planning/phases/01-core-commands/01-05-SUMMARY.md when done
</output>
